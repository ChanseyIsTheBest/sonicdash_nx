/* config.c -- the resolved render size, shared across the port. The settings
 * themselves live in config.txt, read by sd_config.c. MIT. */
#include "config.h"
int screen_width  = 0;
int screen_height = 0;
